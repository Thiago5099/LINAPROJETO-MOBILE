<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lina">

        <!-- Principal -->
        <activity
            android:name=".feture.login.LoginCadastro"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Cardápio -->
        <activity android:name=".feture.cardapio.CardapioMainActivity"/>
        <activity android:name=".feture.cardapio.ReceitaActivity"/>
        <activity android:name=".feture.cardapio.SelecionarPratoActivity"/>

        <!-- Criar Cardápio -->
        <activity android:name=".CriarCardapio.CriarCardapioMainActivity"/>

    </application>

</manifest>